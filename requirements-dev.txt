# Dev / CI / Claude session dependencies SSoT — quickshot 短期客戶 template
#
# Single source of truth for Python dev tooling. All envs install from here:
#   - .github/workflows/rules-lint.yml  → `pip install -r requirements-dev.txt`
#   - .claude/hooks/session-start.sh    → `pip install -q -r requirements-dev.txt 2>/dev/null || true`
#   - Local dev / worktree              → same command as above
#
# Adding / removing a tool? Update this file only.

pytest>=8.0
ruff>=0.5
